class Mosca extends Insecto {
  Mosca(PVector pos, PImage img, int p) {
    super(pos, img, p);
    // Constructor
  }
  
  @Override
  void mover() {
    // Lógica específica de movimiento para mosca
  }
}

class Hormiga extends Insecto {
  Hormiga(PVector pos, PImage img, int p) {
    super(pos, img, p);
    // Constructor
  }
  
  @Override
  void mover() {
    // Lógica específica de movimiento para hormiga
  }
}
